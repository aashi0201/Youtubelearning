{
  "name": "youtubelearning",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "backend:dev": "npm --prefix backend run dev",
    "backend:start": "npm --prefix backend start",
    "backend:check": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Get-ChildItem -Recurse -Filter *.js backend | Where-Object { $_.FullName -notmatch '\\\\node_modules\\\\' } | ForEach-Object { node --check $_.FullName }\"",
    "seed": "npm --prefix backend run seed",
    "test": "npm --prefix backend run test",
    "check": "npm run test && npm run backend:check && npm run frontend:build"
  }
}
